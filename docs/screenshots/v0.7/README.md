# スクリーンショット(v0.7 UI・デモデータのみ)

記事・ピッチ用。すべて自作デモサイト(`http://127.0.0.1:8765/`)に対する実行結果で、
実在のサービス・個人情報は含まない。2026-09-22、Playwright(このリポジトリの既存依存)で撮影
(1280x900、`scripts/start-local.sh`で起動、`SEED_DEMO_DATA=true`のデモアカウントでログイン)。
P8(旧UI部品`.card`/`.badge`/`.chip`等の削除)後の状態。

| ファイル | 画面 |
|---|---|
| `1-login-miruqa.png` | ログイン画面(MiruQA名称) |
| `2-dashboard-miruqa.png` | ダッシュボード(登録プロジェクト数・実行回数・指摘件数・直近の実行一覧) |
| `3-project-miruqa.png` | プロジェクト詳細(対象URL・所有確認/テスト環境宣言の状態・診断開始) |
| `4-plan-miruqa.png` | プラン画面(下見結果・テスト項目書・自然言語での項目追加欄) |
| `5-run-miruqa.png` | 実行状況・結果(合否・カバレッジ・コスト・不具合票・再実行ボタン) |
| `6-findings-miruqa.png` | 不具合一覧(観点・重大度・確度・状態管理と抑制のフィルタ) |

実データ: `runs/samples/run-0b84031e60/`(33件のテスト項目、22件の指摘、実測コスト$0.6072)。
