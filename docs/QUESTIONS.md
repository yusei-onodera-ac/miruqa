# 疑問・確認事項（実装側が追記する。作業は止めず、仮定を明記して進める）

| # | 日時 | 質問・不明点 | 置いた仮定 | 回答（人／別セッション） |
|---|---|---|---|---|
| 1 | 2026-09-20 | `ORCAROUTER_API_KEY` がまだ手元にない(WP0は人の作業待ち)。有効なキーが無いとWP0の技術チェックリスト・WP6のベンチ実測ができない | キーが届くまで、コード側(WP-1〜WP5、WP7の一部)を先に実装し、擬似LLM応答での結合テストで配線を検証しておく。無効キーで疎通確認したところ、エンドポイント自体(`https://api.orcarouter.ai/v1`)は生きており、401が構造化JSONで返ることは確認できた | 未回答 |
| 2 | 2026-09-20 | `docs/contracts.md` の `Run.steps[].verdict` は1ステップ=1アクション想定に見えるが、1LLMターンに複数ツール呼び出しが起こりうる(OpenAI tool calling仕様) | `steps[].verdict` はそのターン中「最後のPolicyVerdict」とし、全アクションの個別ログは新設の `run.json` 直下 `policyLog` 配列に残すことにした。`docs/contracts.md` に追記済み | 未回答（Java層セッションにも影響するため、人経由で共有希望） |
| 3 | 2026-09-20 | Firewall評価API(`POST /api/v1/firewall/evaluate`)のリクエスト/レスポンス形式が未確認(CLAUDE.md記載通り)。専用キー(`ORCA_FIREWALL_KEY`)も未設定 | `agent/policy.py` はFirewallをベストエフォードで呼び、形式が合わない／キーが無い／失敗した場合は自前 `PolicyGate`(許可ドメイン・L4上限・注文確定の承認待ち)にフォールバックする実装にした。どちらを使ったかは `PolicyVerdict.source`(`"firewall"`または`"local"`)に残る | 未回答 |
| 4 | 2026-09-20 | OrcaRouter Guardrails(PIIマスク・`llm_judge`)がまだ未接続(WP0未完のため) | 自前の正規表現マスク(`agent/masking.py`: email/phone/氏名/住所)をLLM送信直前に必ず適用している。Guardrailsが使えるようになったら二重の防御として追加する(既存のマスクは残す) | 未回答 |
| 5 | 2026-09-20 | デモサイトの連打(T-01)を最初にPlaywright経由で試したところ、1クリックしか実際の注文にならなかった(ローカルサーバーが速すぎて、2回目のクリックの頃には既に次ページへ遷移していたため) | `/checkout/confirm` の処理に0.25秒の擬似決済遅延を入れ、実ブラウザ操作でも二重注文を再現できるようにした(実際の決済ゲートウェイの遅延を模したもので、仕込みの本質は変えていない)。デモサイトの仕様変更のため、念のため記載 | 未回答 |
| 6 | 2026-09-20 | 中断・再開(FR-31)のうち、「プロセスが落ちても同じrun_idから続きを再開する」機能(ブラウザの再アタッチ、メッセージ履歴の復元)は未実装。各ステップ後の`run.json`保存(部分結果を残す)はできている | 優先度表の「遅れたら削る順」でFR-31は比較的早い段階で削る候補に入っているため、まずは他のM優先度項目を完了させることを優先した。時間があれば実装する | 未回答 |
| 7 | 2026-09-21 | v0.5: 既存ワーカーAPI(`POST /api/runs {url,goal,persona,mode}`)は、ゴール・ペルソナのAPI完全削除(AC-A)と両立できない | 互換シムは作らず、**破壊的に置き換える**(URL・動詞は極力維持、ボディ形状のみ新契約に変更)。`docs/contracts.md`に「v0.5で破壊的変更」と明記し、影響をJava層セッションへ人経由で伝えてもらう前提で進める | 未回答 |
| 8 | 2026-09-21 | v0.5のJava層(`web/`)UI全面刷新(CHANGE §6)を、このセッション(dev/worker)が担当するか、`dev/web`セッション(既に独立したworktreeが存在)に委ねるか未確認 | 自分の担当ディレクトリ(`agent/`・`checks/`・`bench/`・`report/`・`demo-site/`・`runs/`・`requirements.txt`・`reference/`)のみ編集し、`web/`には手を出さない。`docs/contracts.md`の更新を通じて`dev/web`セッションが追従できるようにする | **回答済み(2026-09-21、別セッション「AIエージェント自社サービス評価」経由でおのゆー決定を伝達)**: Java層のUI全面刷新も引き続きこのセッション(dev/worker)が担当する。`dev/web`セッションは立てない。`web/`の編集もdev/worker側で行ってよい。契約(`docs/contracts.md`)とワーカーAPIを先に固めてから、Java層のUIに着手する順で進める。`main`ブランチから`web/`一式(`git merge main`、コンフリクトなし)を`dev/worker`に取り込み済み(コミット`f9ef364`) |
| 9 | 2026-09-21 | 仕様書抽出ライブラリのバージョン指定希望の有無 | `requirements.txt`には既存の書式(`>=`のみ)に合わせて`python-docx`・`pypdf`を追記する(上限指定なし)。スキャンPDF(OCR)非対応・表/段組みの読み取り精度が低い点は`Spec.warnings`で利用者に明示する | 未回答 |
| 10 | 2026-09-21 | 探索的テストと項目書実行の順序(直列/並行)、ツールセット、自己検証による昇格の要否 | Mustの最小構成では「項目書を全消化→探索」の直列固定とし、探索フェーズのツールセットはP-SECマクロ(`rapid_click`等)を含まない基本操作のみに限定する。`needs_review`→`confirmed`への自己検証・昇格はShould行きとし、Mustでは探索の指摘は`needs_review`のままでよいとする | 未回答 |
| 11 | 2026-09-21 | `TestCase.risk`が`needs_approval`になる基準 | P-SEC(観点)は常に`needs_approval`。P-FLOWのうち、決済・注文確定に関わる手順(手順スキップ・戻る/リロード再送信)を対象とするものだけ`needs_approval`とし、それ以外のP-FLOW(通常の画面遷移確認)は`normal`とする | 未回答 |
| 12 | 2026-09-21 | 仕様書なしモードでの判定確度の扱い | 原則`needs_review`固定とするが、500エラー表示や状態確認(`/__test/state`)など**コード的に明確な根拠が取れるもの**は、仕様の有無に関わらず`confirmed`にしてよいこととする(判定原則「コードの根拠を主とする」を仕様の有無より優先) | 未回答 |
| 13 | 2026-09-21 | デモ用仕様書(`demo-site/spec/`)の内容・粒度 | 自分で起案する。`spec.md`をまず作成し(送料・二重注文防止・数量検証・価格改ざん防止・手順遵守・個人情報同意・未エスケープ表示・エラー詳細非表示・特商法必須項目/導線・表記の正確性、の8章構成)、同内容の`spec.txt`を作る。docx/PDFは時間があれば追加する | 未回答 |
| 14 | 2026-09-21 | `docs/contracts.md`の破壊的変更を、いつ・どうやって`dev/web`セッションに伝えるか | `docs/contracts.md`内に「v0.5で破壊的変更」の節を設けて明記し、`docs/STATUS.md`の更新でも都度触れる。実際にJava層セッションへ伝えるのは人(おのゆー)経由という、既存の役割分担(CLAUDE.md第0章)に従う | **不要になった(2026-09-21)**: #8と同じ伝達により、`dev/web`セッションは立てないことになったため、別セッションへの伝達は不要。`web/`もこのセッションが直接更新する |
| 15 | 2026-09-21 | v0.5の作業方針: 第7章「削る順」を判断の軸にすべきか | 別セッション経由でおのゆー決定を伝達: **時間の制約は今は判断の軸にしなくてよい**。まずはCHANGE-v0.5.mdの要件を満たす形を目指す。Must/Shouldの優先順位そのもの(M0〜M7の作業順)は、依存関係上の合理性としてそのまま使う(先にAPIを固めてからUIに着手する、等) | 回答済み |
| 16 | 2026-09-21 | `POST /api/plans`(下見+項目書生成)のバックグラウンドスレッドが、原因未特定のまま約21分ハングした事例が1件発生(HTTP自体は応答し続けていたが、`status`が`recon`のまま進まなかった) | **原因判明・修正済み**: (a) `ORCAROUTER_API_KEY`が2026-09-21 05:xx頃から`401 Invalid API key`を返すようになっており(#17参照)、LLM呼び出しが軒並み失敗/タイムアウトし、7画面ぶん(1画面あたり最大`LLM_MAX_RETRIES`回のリトライ)を順に待つ設計だったため非常に遅くなっていた。(b) さらに、全画面でテスト項目が0件になった場合、`Plan.status`が`ready`にも`failed`にもならず`recon`のまま固定されるバグがあった(`agent/planning.py`)→ 修正: ①下見(`site_map`)が終わっていれば`status`は必ず`ready`にする(0件でも)。空になった理由は`Plan.note`に記録(`docs/contracts.md`に追記、`plan.html`にも表示)。②画面2つ連続でLLM呼び出しが復旧できない場合は、系統的な障害とみなして残り画面のリトライを打ち切る(`generate_test_cases`の`consecutive_degraded`) | 解決済み |
| 17 | 2026-09-21 05:3x頃 | **[要人対応・緊急]** `ORCAROUTER_API_KEY`が`401 Invalid API key`を返すようになった。それまで(M1〜M6、MWの実LLM検証)は正常に動いていたが、突然すべてのLLM呼び出しが401で失敗するようになった(`agent/llm.py`ごしに素のpingでも再現)。原因は不明(キーの失効・ローテーション・OrcaRouter側の障害等、いずれも実装側からは判別できない) | キーの有効性はこちらでは復旧できないため、コード側の対処(早期打ち切り・ステータス修正、上記#16)のみ行い、LLMを使わない作業(ドキュメント整理・受入基準の確認・コードレビュー)を優先して進めている。**人によるキーの確認・再発行が必要** | **解決済み(2026-09-21)**: おのゆー対応によりキー復旧、Named Router `orcarouter/site-inspector`も作成済み。復旧後、A/B/C 3構成での比較(#19参照)まで実施した |
| 18 | 2026-09-21 05:5x頃 | `dev/worker`の区切りごとのコミットを`main`へローカルマージするよう指示を受けたが(別セッション経由)、`main`ブランチが**チェックアウトされている別のworktree(`/Users/onoderayusei/AI_HACK_2026`)に、コミットされていない変更(`CLAUDE.md`・`docs/requirements.md`の変更、`docs/CHANGE-v0.5.md`・`docs/OVERNIGHT-LOG.md`の未追跡ファイル)がある**ことを確認した。おそらく別セッション(夜間見張り/企画セッション)がそのworktreeで作業中 | 他セッションの未コミットの作業を巻き込む・上書きするリスクを避けるため、**`main`へのマージは今回見送った**。`dev/worker`側のコミット(M0〜M6、MW、バグ修正)はすべて完了しており、`main`が片付き次第(そのセッションがコミットするか、人が確認してから)マージするのが安全と判断した | 未回答(`main`ワークツリーの状況を確認いただき、マージのタイミングを指示してほしい) |
| 19 | 2026-09-21(コスト記録漏れ調査中) | **#17の続報**: 別セッション経由で「`ORCAROUTER_API_KEY`の利用上限($5)を使い切った」との報告を受けた。あわせて`agent/ledger.py`(新設)の動作確認のため、実際に`python -m agent.specs demo-site/spec/spec.md --save`を1回実行して切り分けたところ、応答は**401ではなく403 `model_access_denied`(`reason: block_key_scope`)**で、「このAPIキーは`google/gemini-3.5-flash`にアクセスできない」という内容だった(判断11で一度成功実績のあったモデル)。つまり#17を書いた時点(401=キー自体が無効)から、いま(403=キーは通るがモデルへのアクセス権がスコープ外)へ、エラーの種類が変わっている | この呼び出しは403で即座に失敗しており(課金なし、`agent/llm.py`の401/403即時diverge実装により再試行もしていない)、コストは発生していない。原因はこちらでは断定できないが、「利用上限到達時にコンソール側がキーのモデルスコープを自動的に絞った」可能性を仮説として残す。当面はP-A(コスト台帳・上限)の実装をライブ呼び出しなしで進め、ライブ検証は次の一言をもらってから行う | **解決済み(2026-09-21)**: おのゆーからキー復旧・Named Router作成済みの連絡を受け、A(`google/gemini-3.5-flash`直指定)/B(`orcarouter/site-inspector`)/C(`orcarouter/auto`)の3構成で煙テスト(401/403なし)→本比較を実施した。結果、削減効果は確認できなかった(全構成の呼び出しが100%高難度プールのモデルに解決)。詳細は`docs/design-decisions.md`判断16、`docs/STATUS.md`「ルーター比較の実測結果」 |
| 20 | 2026-09-21(ルーター再実験・フォールバック実演中) | **[人への確認依頼]** ① セッション固定(Session affinity)のオン/オフでの比較をお願いしたい。`X-Orca-Session-Tier`ヘッダーは今回の実験でも一貫して空だったため、こちらではオン/オフの実測比較ができない。コンソール側の設定を教えていただくか、切り替えて比較実行の許可をいただきたい。② フォールバック実演(FR-32)で`openai/gpt-4o`・`google/gemini-2.5-flash`を直接指定したところ、いずれも403 `model_access_denied`(`block_key_scope`)だった。このキーで直接呼べるのは`google/gemini-3.5-flash`と2つのrouter名(`orcarouter/auto`、`orcarouter/site-inspector`)だけのようで、**別ベンダー(OpenAI等)への実際のフォールバックはこのキーのスコープでは検証できなかった**。代わりに`orcarouter/site-inspector`自体をフォールバック先にした場合は3/3成功し、内部で`google/gemini-2.5-flash`に解決されることを確認した(同一ベンダー内の代替という限定的な実演に留まる) | フォールバック自体の仕組み(agent/llm.pyの2段フェーズ、`_maybe_inject_fault`のフェーズ限定化)は実装・実測済み。別ベンダーへの切替を実演したい場合は、①このキーで`openai/*`等、別ベンダーのモデルへのアクセスを許可いただくか、②`ORCA_FALLBACK_MODELS`に許可済みの別モデルを指定いただければ、その構成で再実演できる | **②(別ベンダーアクセス)は解決済み(2026-09-21同日)**: おのゆーが`openai/gpt-4o`へのアクセスを許可。再実測したところ3/3完走、`resolvedModel: gpt-4o-2024-08-06`でGoogle→OpenAIへの実際の切替を確認(`docs/design-decisions.md`判断18追記、`bench/results/bench-20260921T050000Z-fallback-demo.json`)。ただし`X-Orca-Fallback-Model`ヘッダーは今回も付与されなかった(このアプリのクライアント側フォールバック方式では原理的にヘッダーが立たないと判明。同ファイル参照)。**①(セッション固定)はおのゆーが対応中**: オフでの実測であることを既存のbench結果(判断16)に明記し、ON時の比較は別途1回実施予定(`docs/design-decisions.md`「セッション固定ON/OFF比較」参照) |
| 21 | 2026-09-21 14:3x頃 | **[要確認]** おのゆーから「ワーカーの`.env`の`LLM_BUDGET_PER_DAY_USD`を$3.0に変更しサーバーを再起動済み」との連絡を受けたが、実際に`.env`を確認したところ`LLM_BUDGET_PER_DAY_USD=13.0`だった(3.0ではない)。さらに、そのときに稼働していたワーカープロセス(`agent.server`)は、その`.env`の編集(14:32:48)より前(12:55:56)に起動しており、編集後の値(3.0か13.0かに関わらず)を一度も読み込んでいなかった。**この時点では、おのゆーが意図したどちらの上限も実際には反映されていなかった** | ワーカーの`.env`は書き換えず(「勝手にこの上限を書き換えない」という後続の指示を尊重)、プロセスだけ再起動して現在の`.env`の値(13.0)を反映させた(値そのものの真偽は判断できないため、書き換えは行わない)。その後、おのゆーから「OrcaRouterの残クレジットは約$13」という訂正と、「止める基準は`.env`の値(いまは$3.0)、80%到達で連絡」という新方針を受けたため、今後は`.env`の値をそのつど確認し、変更があれば都度サーバーを再起動して反映させる運用にする | **解決済み(2026-09-21)**: おのゆーから、`13.0`が意図した値(クレジット残り約$13の全額を日次上限にする意図)との回答。書き換えず、80%(約$10.4)到達時に連絡する運用にする |
| 22 | 2026-09-21(CHANGE-v0.6.md着手) | **v0.6着手時に置いた仮定(第9章のプロンプト通り、確認を待たず進める)**: ①DBはH2ファイルモード(Postgresは別プロセス管理のリスクを避けるため見送り)。②ジョブキューはDBを正としつつ、ワーカーへの割り当ては既存の同期HTTP呼び出しを流用(ワーカーをポーリング型に作り直さない)。③ワーカーAPI認証は環境変数の共有秘密+専用ヘッダー(署名・失効の仕組みは省略)。④ドメイン所有確認は`/.well-known/`ファイル方式のみ実装(DNS TXTは伝播遅延リスクからロードマップ行き)。⑤秘密情報の暗号鍵は環境変数の固定鍵(KMS・ローテーションなし)。⑥UIは引き続きThymeleaf+素のJS、グラフはインラインSVG(新規JSライブラリを追加しない)。⑦第0a章と第2章/D-1で矛盾している`invitations`・`api_tokens`テーブルは、第0a章の優先(「対象外とした項目は各フェーズでも作らない」)に従い両方とも作らない。⑧リトライは真の途中再開ではなく新しい試行としてやり直す(`resume_plan`による項目書生成の継続だけは例外として残す)。⑨2つ目のデモサイト(Q-6)はログイン・モーダル・iframeのみの最小構成。⑩バックアップ復元(AC-D2)はローカル開発データで実際に実施する。⑪サービス名は未定のためプレースホルダー設定値として扱う。⑫同時実行数は第0a章の読み替え通り2件とする(第2章本文・AC-O1の「3件」表記より第0a章を優先) | 上記の仮定のまま、確認を待たずP1(DB・認証・組織・テナント分離・監査ログ・ワーカーAPI認証)から順に進める | 未回答(仮定に誤りがあれば指摘してほしい。とくに⑦の`invitations`/`api_tokens`省略と、DBエンジンの選択) |
